using System;
using System.Net;
using System.IO;

class Program
{
    static void Main()
    {
        WebClient client = new WebClient();
        
        client.DownloadFile("https://people.sc.fsu.edu/~jburkardt/data/csv/addresses.csv", "test.csv");
        
        string[] lines = File.ReadAllLines("test.csv");
        
        Console.WriteLine("Содержимое файла:");
        for (int i = 0; i < 10 && i < lines.Length; i++)
        {
            Console.WriteLine(lines[i]);
        }
    }
}
